services:
  - type: web
    name: aqi-inference-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn pipelines.inference_pipeline:create_app --factory --port $PORT
    envVars:                                                                    
      - key: PYTHONUNBUFFERED
        value: "1"
